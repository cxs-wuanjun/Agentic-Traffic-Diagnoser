# TransAI 项目 · 简历描述方案

---

## 一、如何在简历中定位这个项目

在撰写简历之前，先明确这个项目对招聘方的**核心价值信号**：

| 能力维度 | 项目中的体现 | 面向岗位 |
|---|---|---|
| **LLM 应用工程** | ReAct Agent 多工具调度、Prompt 工程 | AI 应用工程师、后端工程师 |
| **RAG 系统设计** | ChromaDB + 嵌入模型 + MD5 去重全链路 | AI 工程师、NLP 工程师 |
| **系统架构能力** | Middleware 拦截、动态上下文注入、状态机 | 后端架构、全栈工程师 |
| **工程质量意识** | 全链路 TraceLogger 可观测性、滑动窗口记忆管理 | 任何研发岗位 |
| **垂直领域落地** | 交通诊断、数据模拟、报告生成业务完整闭环 | 有行业背景优先的岗位 |

---

## 二、项目名称（建议）

```
TransAI · 交通智能诊断与决策 Agent 平台
```

---

## 三、简历描述（三个版本）

### ✅ 版本 A：通用 AI 工程师 / LLM 应用方向（推荐）

> 适合投递：AI 应用工程师、大模型工程师、后端开发（AI 方向）

---

**TransAI · 交通智能诊断 Agent 平台**  
`Python` `LangChain` `LangGraph` `ChromaDB` `Streamlit` `Qwen / DashScope`

基于 **ReAct（Reasoning + Acting）架构**设计并独立实现的垂直领域 AI 智能体平台，具备自主多工具调度、知识增强检索（RAG）和迭代自我纠错能力：

- **ReAct 多工具路由**：封装 6 个业务工具（路况查询、事件记录、报告生成等），通过 `@tool` 描述语义实现大模型自主调度，完成"思考 → 行动 → 观察"闭环
- **Middleware 动态上下文注入**：利用 LangChain 生命周期钩子实现 System Prompt 运行时热切换，在同一会话内支持"普通对话"与"严格报告模式"的零耦合切换
- **Reflection 自我纠错**：自研 3 轮迭代反思状态机（Draft → Review → Refine），通过角色切换让大模型担任"作者"与"审查专家"，有效抑制幻觉、提升报告质量
- **本地 RAG 知识库**：集成 ChromaDB + 阿里 `text-embedding-v4`，实现文档解析、递归分块（chunk=400, overlap=50）、MD5 去重入库全流程
- **全链路可观测性**：自研 `TraceLogger`，在 Middleware 层捕获每次工具调用的入参、出参与耗时，前端实时渲染，将 Agent 黑盒行为可视化
- **上下文工程**：基于滑动窗口（保留最近 5 轮）的 `HistoryManager`，在防止 Token 爆炸的同时保持对话连贯性

---

### ✅ 版本 B：简洁版（适合篇幅受限的简历）

> 适合：简历空间有限，需要高度凝练

---

**TransAI · 交通智能诊断 Agent 平台**  
`Python` · `LangChain / LangGraph` · `ChromaDB` · `Streamlit`

独立设计并实现基于 **ReAct 架构**的垂直 AI Agent，覆盖多工具自主调度、本地 RAG 知识增强、Reflection 迭代自纠错与全链路 Trace 可观测性；集成阿里通义千问模型，构建完整的交通诊断报告生成闭环。

---

### ✅ 版本 C：偏后端/系统方向（强调工程设计）

> 适合投递：后端工程师、平台工程师、有系统设计考察的岗位

---

**TransAI · 交通智能诊断 Agent 平台**  
`Python` `LangChain` `ChromaDB` `Streamlit`

为解决 LLM Agent 在生产环境中的**可控性与可观测性**问题，独立设计并实现以下工程模块：

- **Middleware 拦截层**：基于 LangChain 生命周期钩子（`before_llm` / `after_tool`），实现请求上下文的无侵入注入与动态 Prompt 热切换，消除多任务模式下的指令冲突
- **Reflection 状态机**：纯手写迭代状态机，避免引入重型框架，通过"Draft-Review-Refine"3 轮循环与系统角色切换，将报告质量提升显著
- **RAG 全链路**：文档解析（PDF/TXT）→ 递归分块 → 向量化存储（ChromaDB）→ MD5 去重，并将 RAG 作为普通工具嵌入 ReAct 循环，避免检索与推理的耦合
- **TraceLogger 可观测链路**：在 Agent 执行层采集工具调用链（名称、入参、耗时、结果），存入内存队列并在前端实时渲染，实现 Agent 行为白盒化

---

## 四、面试话术准备（高频问题）

以下是面试官最可能追问的技术点，建议结合代码提前准备好回答：

### Q1：你的 ReAct 是怎么实现的？大模型怎么知道调哪个工具？

> **答**：每个工具通过 `@tool` 装饰器封装，关键在于 `description` 字段的语义描述。大模型不看代码，只看描述来做语义匹配。如果模型调错工具，解决办法是优化 description，而不是改代码逻辑——这本质上是"对齐大模型的认知边界"。

### Q2：Reflection 迭代会不会很慢？你怎么权衡次数？

> **答**：当前设置 2 轮迭代（可配置），每轮额外增加 2 次 LLM 调用。经测试 2 轮已能覆盖大部分幻觉和格式问题；更多轮收益递减且延迟显著增加。这是工程上的 trade-off，不是无脑追求次数。

### Q3：RAG 为什么不用 pipeline 模式，而是把它当工具挂在 ReAct 里？

> **答**：传统 RAG pipeline 会对所有问题都先检索，导致事实性查询（如"现在路况"）被强制走向量库，引入无关噪声。把 RAG 作为一个工具嵌入 ReAct，让大模型自己判断"什么时候需要查书"，规避了检索与推理的不匹配问题。

### Q4：你的内存管理怎么防 Token 爆炸？

> **答**：基于滑动窗口（FIFO），只保留最近 5 轮对话。不用摘要压缩是因为它会引入额外 LLM 调用和摘要精度损耗，对本项目场景来说滑动窗口已经足够且实现简单。

---

## 五、项目亮点总结（自我介绍/口头表达用）

> 可用于 1-2 分钟的项目介绍

"TransAI 是我独立设计和实现的一个垂直领域 AI Agent 项目。它的核心是一套基于 ReAct 架构的大模型调度引擎，能让模型自主决定调哪个工具。在工程层面，我重点解决了三个问题：一是大模型的幻觉问题，通过自研的 Reflection 状态机实现 3 轮迭代自我纠错；二是多任务模式下的指令冲突问题，通过 Middleware 拦截层实现 System Prompt 的运行时热切换；三是 Agent 的黑盒问题，通过全链路 TraceLogger 把每次工具调用的行为都记录和可视化出来。这个项目让我深刻理解了企业级 AI 应用工程的核心挑战，和纯调 API 的玩具项目有本质区别。"
